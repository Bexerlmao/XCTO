<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.bexerlmao.xcto.question.mapper.QuestionMapper">
    
    <!-- 插入题目 -->
    <insert id="insertQuestion" parameterType="cc.bexerlmao.xcto.question.pojo.Question">
        INSERT INTO question (
            class_id,
            question_type,
            question,
            options,
            answer,
            create_time,
            update_time
        ) VALUES (
            #{classId},
            #{questionType},
            #{question},
            CAST(#{options, typeHandler=com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler} AS jsonb),
            CAST(#{answer, typeHandler=com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler} AS jsonb),
            CURRENT_TIMESTAMP,
            CURRENT_TIMESTAMP
        )
    </insert>
    
    <!-- 批量插入题目 -->
    <insert id="batchInsertQuestion" parameterType="java.util.List">
        INSERT INTO question (
            class_id,
            question_type,
            question,
            options,
            answer,
            create_time,
            update_time
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.classId},
                #{item.questionType},
                #{item.question},
                CAST(#{item.options, typeHandler=com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler} AS jsonb),
                CAST(#{item.answer, typeHandler=com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler} AS jsonb),
                CURRENT_TIMESTAMP,
                CURRENT_TIMESTAMP
            )
        </foreach>
    </insert>
    
    <!-- 查询题目 -->
    <select id="selectQuestionById" parameterType="Long" resultType="cc.bexerlmao.xcto.question.pojo.Question">
        SELECT * FROM question WHERE id = #{id}
    </select>

    <!-- 随机获取班级下的一道题目 -->
    <select id="selectRandomQuestionByClassId" parameterType="Long" resultType="cc.bexerlmao.xcto.question.pojo.Question">
        SELECT * FROM question WHERE class_id = #{classId} ORDER BY RANDOM() LIMIT 1
    </select>
    
    <!-- 查询所有题目 -->
    <select id="selectAllQuestions" resultType="cc.bexerlmao.xcto.question.pojo.Question">
        SELECT * FROM question ORDER BY create_time DESC
    </select>
    
    <!-- 根据班级ID查询题目 -->
    <select id="selectQuestionsByClassId" parameterType="Long" resultType="cc.bexerlmao.xcto.question.pojo.Question">
        SELECT * FROM question WHERE class_id = #{classId} ORDER BY create_time DESC
    </select>
    
    <!-- 更新题目 -->
    <update id="updateQuestion" parameterType="cc.bexerlmao.xcto.question.pojo.Question">
        UPDATE question SET
            class_id = #{classId},
            question_type = #{questionType},
            question = #{question},
            options = CAST(#{options, typeHandler=com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler} AS jsonb),
            answer = CAST(#{answer, typeHandler=com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler} AS jsonb),
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>
    
    <!-- 删除题目 -->
    <delete id="deleteQuestion" parameterType="Long">
        DELETE FROM question WHERE id = #{id}
    </delete>
</mapper>
