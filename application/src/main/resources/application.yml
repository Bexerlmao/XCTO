spring:
  application:
    name: Application
  datasource:
    url: jdbc:postgresql://localhost:5432/xcto
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 123456

server:
  port: 8081

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: cc.bexerlmao.question.entity
